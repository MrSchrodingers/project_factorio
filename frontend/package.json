{
  "name": "factorio-ai-lab-map",
  "private": true,
  "version": "0.12.0",
  "type": "module",
  "description": "Client-side canvas renderer for the live Factorio factory map",
  "scripts": {
    "build": "tsc --noEmit && vite build && node stamp.mjs",
    "typecheck": "tsc --noEmit",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
